@{
    ViewData["Title"] = "Access Denied";
}

<div class="auth-page access-denied-page">
    <div class="auth-card access-denied-card">
        <span class="page-eyebrow">NEXUS IT / SECURITY</span>
        <div class="access-denied-icon">!</div>
        <h1>Access restricted</h1>
        <p>Your current NexusIT role does not have permission to access this area.</p>
        <a asp-controller="Home" asp-action="Index" class="nexus-primary-button">Return to Dashboard</a>
    </div>
</div>
